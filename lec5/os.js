//os feature 
let os = require("os");
console.log(os.arch());
console.log(os.platform());
console.log(os.networkInterfaces());
console.log(os.cpus()); // to know about CPU INFO

// basically this os module is used to run some specific task related to some specific os requirement 